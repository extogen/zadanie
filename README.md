cd Desktop/newproject

git init

git config --global user.name "Ivan Kuchuhura"

git config --global user.email "kuchuhuraivan@gmail.com"

git add .

git commit -m "add Main.java"

git branch hotfix

git branch develope

git branch -m master realease

git remote add origin git@github.com:extogen/zadanie.git

git push origin realease

git push origin hotfix

git push origin develope
